## 🎨 **LEVEL 1: CSS FUNDAMENTALS**

### 1. CSS Basics & Syntax

- CSS syntax: selectors, properties, values
- Three ways to add CSS: inline, internal (`<style>`), external (`.css` file)
- Comments: `/* comment */`
- CSS specificity basics

### 2. Selectors

- Element selectors: `p`, `div`, `h1`
- Class selectors: `.class-name`
- ID selectors: `#id-name`
- Universal selector: `*`
- Grouping selectors: `h1, h2, p`

### 3. Colors & Backgrounds

- Color formats: named, hex (`#ff0000`), RGB, RGBA
- `color` property (text color)
- `background-color`
- `background-image`, `background-size`, `background-position`
- `background-repeat`

### 4. Text Styling

- `font-family`, `font-size`, `font-weight`
- `font-style` (italic)
- `text-align`, `text-decoration`, `text-transform`
- `line-height`, `letter-spacing`
- Google Fonts integration

---

## 📦 **LEVEL 2: BOX MODEL & LAYOUT BASICS**

### 5. The Box Model

- `width`, `height`
- `padding` (inner spacing)
- `margin` (outer spacing)
- `border` (border-width, border-style, border-color)
- `box-sizing: border-box` (essential!)

### 6. Display Property

- `display: block`
- `display: inline`
- `display: inline-block`
- `display: none`

### 7. Positioning

- `position: static` (default)
- `position: relative`
- `position: absolute`
- `position: fixed`
- `position: sticky`
- `top`, `right`, `bottom`, `left`, `z-index`

---

## 🎯 **LEVEL 3: MODERN LAYOUT (Critical!)**

### 8. Flexbox

- `display: flex`
- Container properties: `flex-direction`, `justify-content`, `align-items`
- `flex-wrap`, `align-content`, `gap`
- Item properties: `flex-grow`, `flex-shrink`, `flex-basis`, `align-self`
- **Practice this heavily!** Most used layout method

### 9. CSS Grid

- `display: grid`
- `grid-template-columns`, `grid-template-rows`
- `gap`, `row-gap`, `column-gap`
- `grid-column`, `grid-row` (spanning)
- `grid-template-areas` (named areas)
- `justify-items`, `align-items`
- **Critical for page layouts!**

---

## 📱 **LEVEL 4: RESPONSIVE DESIGN**

### 10. Media Queries

- `@media` syntax
- Breakpoints: `min-width`, `max-width`
- Common breakpoints: 640px, 768px, 1024px, 1280px
- Mobile-first vs Desktop-first approach
- **Essential for modern web!**

### 11. Responsive Units

- Absolute: `px`
- Relative: `%`, `em`, `rem` (use rem!)
- Viewport: `vw`, `vh`, `vmin`, `vmax`
- When to use each unit

### 12. Responsive Images

- `max-width: 100%`
- `object-fit`, `object-position`
- Responsive background images

---

## 🎭 **LEVEL 5: ADVANCED STYLING**

### 13. Pseudo-classes

- `:hover`, `:active`, `:focus`
- `:first-child`, `:last-child`, `:nth-child()`
- `:disabled`, `:checked`
- Form states: `:valid`, `:invalid`, `:required`

### 14. Pseudo-elements

- `::before`, `::after`
- `::first-letter`, `::first-line`
- `::placeholder`
- `content` property

### 15. Transitions & Animations

- `transition` property (duration, timing, delay)
- `transform`: `translate`, `scale`, `rotate`, `skew`
- `@keyframes` for animations
- `animation` property
- **Great for UI polish!**

---

## 🔧 **LEVEL 6: SELECTORS & COMBINATORS**

### 16. Advanced Selectors

- Descendant: `div p`
- Child: `div > p`
- Adjacent sibling: `div + p`
- General sibling: `div ~ p`
- Attribute selectors: `[type="text"]`, `[class*="btn"]`

### 17. Specificity & Cascade

- Specificity calculation (inline > ID > class > element)
- `!important` (avoid when possible)
- Inheritance
- CSS cascade rules

---

## 🚀 **LEVEL 7: MODERN CSS FEATURES**

### 18. CSS Variables (Custom Properties)

- Defining: `--main-color: #3490dc;`
- Using: `color: var(--main-color);`
- Scope (`:root` vs local)
- **Very useful in Next.js!**

### 19. Modern Layout Features

- `aspect-ratio`
- `clamp()` for responsive typography
- `min()`, `max()` functions
- `calc()` for calculations

### 20. Shadows & Effects

- `box-shadow`
- `text-shadow`
- `opacity`
- `filter` (blur, brightness, contrast, etc.)
- `backdrop-filter` (glassmorphism)

---

## 🎨 **LEVEL 8: TAILWIND CSS**

### 21. Tailwind Fundamentals

- Utility-first approach
- Common utilities: spacing, colors, typography
- Responsive prefixes: `sm:`, `md:`, `lg:`, `xl:`
- Hover/focus states: `hover:`, `focus:`
- **Next.js default choice!**

### 22. Tailwind Advanced

- Custom configurations
- Theme customization
- Using `@apply` directive
- Dark mode: `dark:` prefix
- Component patterns with Tailwind

---
